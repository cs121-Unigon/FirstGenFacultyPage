# FirstGenFacultyPage

## Installation Instructions
* `git clone <repo>`
* `cd into/repo`
* `bundle install`
* `rails db:migrate`
* `rails db:seed`
* fill in config/application.yml with information
* `rails server`
